# DuoLA AgentCost 产品级审计与竞品对比

> 审计日期：2026-08-14  
> 审计结论：本地 Gateway 已达到可运行的产品级 release candidate；本轮补齐了远程 Gateway 认证、请求体读取前上限、流式取消记账、空闲超时、Token/USD 并发预算预留、缓存总量与过期清理、非幂等 Fallback 保护、自定义 Profile 数据隔离和双监听共享优雅退出。真实 Provider/Agent 兼容矩阵和跨平台安装包仍是公开发布门禁，不把云端多租户、语义缓存和自动输出改写冒充已完成。

## 一、先给结论

### 已经能确认的

- Rust 单二进制可以构建。
- Gateway 可以透传 JSON 和 SSE 流。
- Provider 可以通过环境变量取 Key，Fallback 现在只允许同一协议族。
- 工具结果规则只处理明确的 `tool`/`tool_result`/`function_call_output` 内容：ANSI 清理和连续重复日志折叠默认开启，工具结果上限必须显式配置；不碰用户文本、代码参数和未知 JSON 字段。
- SQLite 账本、Provider usage、缓存 Token、费用估算、预算预检查、重复请求暂停、bypass、Codex 配置快照恢复均有本地自动化覆盖。
- 自定义 `--config` 的预算重载路径已经与运行中的 Gateway 对齐。
- 所有 Provider、预算、缓存、路由命令和 `launch` 都支持自定义 `--config`，托管子进程使用同一配置文件。
- 非流式响应和 SSE 响应都有硬字节上限；SSE 不再只扫描前 2 MiB，终止事件和 usage 可以在大响应尾部被识别。
- 缓存命中在重复请求、并发和 Token/美元预算之后执行；缓存键包含凭证/租户相关请求头哈希，只缓存确定性 JSON 响应；命中更新 LRU 顺序且不消耗上游限流额度。
- Gateway 启动时会把遗留的 `running` 请求和 Provider 尝试标为 `interrupted`；Admin 只允许回环地址。
- Gateway 非回环监听必须配置环境变量 Token，并通过 `X-DuoLA-Gateway-Token` 认证；请求体由框架和应用双重限制。
- 流式客户端断开、Provider 流式错误、空闲超时和响应超限都会完成一次且仅一次账本结算；缓存有条数、单条大小和总字节上限。
- USD 预算在上游发送前使用最坏 Provider 价格预留；未知协议的非幂等 Fallback 默认关闭。
- Session/Daily Token 预算同样在上游发送前预留，并在完成、取消或失败后释放；不会因并发预检查竞态而共同超限。
- Provider/预算/缓存/路由通过 reload 更新；自定义 `--config` 的 ledger、bypass 和 Codex snapshot 数据目录彼此隔离。
- 显式模型映射会按用户配置改写模型并选择对应 Provider；Fallback 每次尝试、失败原因和最终 Provider 可通过请求详情 API 查询。
- `launch` 托管模式会监测 Gateway，异常退出后最多自动重启 3 次；手动 `serve` 仍需显式 bypass/restore。

### 不能宣称已经完成的

1. **不能宣称达到 Edgee 的固定压缩百分比。** 当前内置变换是确定性的 ANSI 清理、连续重复日志折叠、等价 JSON 紧凑化、同请求重复结果引用和保守工具面裁剪；另有显式 opt-in 的结果上限。真实节省必须以同任务双跑证明。
2. **不能宣称真实节省已经被证明。** 我们记录的是变换后的 Provider usage 和字节估算；没有同一个真实 Agent/真实模型的原始路径基线，所以不能从当前账本推导真实节省金额。
3. **不能宣称进程崩溃自动 Fail Open。** 正常 `launch` 退出会恢复 Codex 配置；强制终止 Gateway 后，已经指向本地端口的 Agent 请求可能失败，需要用户执行 bypass/restore。
4. **不能宣称原文可恢复。** receipt 保存 hash、规则 ID、字节数和状态，不保存 Prompt、代码或工具原文，账本不能重建原文。
5. **不能宣称所有真实客户端已全部验收。** LinkAPI OpenAI-compatible Endpoint 与真实 Codex CLI 已完成一轮验收；原生 OpenAI/Anthropic、Claude Code、Cursor BYOK 及持续兼容矩阵仍需人工验收。
6. **不能宣称自动按成本选择任意模型。** 当前 `routing=cost` 只在用户显式配置单价时，对同协议、可选显式成本池中的 Provider 按输入价格排序；模型映射仍必须显式配置，不存在隐藏的便宜模型降级。

## 二、实际运行检查

本次执行结果：

```text
cargo test --all-targets --all-features                 PASS
cargo clippy --all-targets --all-features -- -D warnings PASS
./tests/e2e.sh                                          PASS
cargo build --release                                  PASS
cargo package --allow-dirty --no-verify                 PASS
```

端到端脚本覆盖：

- ANSI 工具结果变换和用户字段保留；
- 非流式响应；
- SSE 流式响应和 usage；
- Anthropic 风格 `message_start/message_delta/message_stop` 流式事件与分段 usage；
- 终止事件晚于 2 MiB 的长 SSE；
- 5xx 后同协议 Fallback；
- 重复请求暂停；
- 预算/循环拒绝进入账本并在 Dashboard 统计；
- bypass 后原样转发；
- Codex 配置接管、退出恢复；
- 账本费用、实测 usage 和 receipt；
- Fallback 尝试链与 `/api/requests/{id}` 请求详情；
- 运行中预算热加载和预算拒绝。

但这仍然是**协议和本地运行时验收**，不是第三方真实 Agent 生产验收。

## 三、与相邻产品的事实对比

| 能力 | DuoLA AgentCost 当前实现 | Edgee | LiteLLM | Portkey |
|---|---|---|---|---|
| 主要定位 | 本地 BYOK relay、账本、预算、有限确定性规则 | 面向 Coding Agent 的压缩、路由、观测 Gateway | 100+ 模型统一 API、Proxy、路由和成本管理 | 统一 Gateway、Fallback、预算、观测、治理 |
| Agent CLI | `serve`/`launch`，Codex 配置恢复 | `edgee launch claude/codex/opencode` 等 | 主要是 Proxy/SDK/部署，不是同样的本地 Coding Agent 启动器 | 有 Agent CLI，可为 Codex/Claude Code 配置 Gateway |
| 工具结果压缩 | ANSI、重复日志、等价 JSON、同请求重复结果引用；显式上限 | Tool Result Trimming，另有 Tool Surface Reduction、Output Brevity | 通用网关能力，非其核心 Coding Agent 压缩卖点 | 通用 Gateway，重点在路由/治理/观测 |
| 工具面裁剪 | 大工具面、任务明确时确定性筛选，含核心 Agent 工具保护 | 已有方案 | 未作为本项目核心能力 | MCP Gateway/治理，不等于本地工具面压缩 |
| 多 Provider | 本地配置，显式 model_map、priority/cost 路由、同协议 Fallback，尝试链可查 | 多 Provider 路由和自动 fallback | 多 Provider、统一格式、Router、Fallback | 多 Provider、Fallback、Load Balance、Conditional Routing |
| 费用统计 | 本地 SQLite；Provider usage 优先；无 usage 时估算 | Session/Team 观测和节省报告 | 多租户成本、项目/用户 Spend 管理 | 成本、Token、Trace、OTel、团队报表 |
| 预算 | 请求/会话/日 Token 与美元预算、重复请求、每分钟限流、并发上限 | 团队/计划级控制 | 项目/用户/Key 等预算和限流 | 成本/Token budget、rate limit、告警 |
| 缓存 | 显式安全精确缓存；工具、流式、执行和实时请求默认排除；TTL/LRU/清空 | 更完整的缓存/压缩器 | Proxy/缓存生态 | Simple + Semantic Cache |
| 原文/隐私 | 默认不保存原文，receipt 只存 hash | 依赖其 Gateway/账户隐私策略 | 典型 Proxy/云或自托管配置 | SaaS、混合或自托管方案，按产品配置决定 |
| 团队治理 | 未实现 | Team 观测和管理 | Key、项目、团队、Dashboard | Access、Guardrails、MCP Gateway、Audit Logs |
| 当前差异 | 本地、简单、BYOK、不托管额度、可查看规则证据 | 更完整的压缩产品和用户验证 | 更成熟的通用 Provider Gateway | 更成熟的企业 Gateway 和治理 |

事实来源：

- [Edgee Token Compression](https://www.edgee.ai/docs/features/token-compression)：公开了 Tool Result Trimming、Tool Surface Reduction alpha、Output Brevity 和压缩指标。
- [Edgee Agent Gateway](https://www.edgee.ai/coding-agents)：公开了 Coding Agent CLI、路由、Fallback、观测和多 Agent 支持矩阵。
- [LiteLLM 官方文档](https://docs.litellm.ai/)：公开了 100+ Provider 统一格式、Router/Fallback、Proxy、Spend 和预算能力。
- [Portkey AI Gateway](https://portkey.ai/docs/product/ai-gateway)：公开了统一 API、缓存、Fallback、重试、熔断、负载均衡、预算和限流。
- [Portkey Coding Agents](https://portkey.ai/docs/product/coding-agent)：公开了 Coding Agent 接入、团队预算、Fallback、MCP Gateway、Guardrails 和 Audit Logs。
- [Anthropic LLM Gateway](https://docs.anthropic.com/en/docs/claude-code/llm-gateway)：官方说明 Claude Code 可以通过 Gateway 获得认证、用量、预算和路由能力，但也明确 LiteLLM 是第三方实现。

## 四、产品定位审计

### 当前定位可以成立的部分

“本地优先的 Agent Runtime Relay”是成立的：用户不交出模型 Key，Provider 请求在本机转发，本地有账本、预算和可解释规则。这与 LiteLLM/Portkey 的集中式团队 Gateway、Edgee 的完整压缩 Gateway 不完全相同。

### 当前定位不能拔高的部分

“Agent 成本优化层”必须以真实双跑数据证明，不能用固定比例宣传。本版本已具备工具面裁剪、重复结果引用、精确缓存、预算和路由等机制，但对相似语义缓存和模型输出不做默认改写。

因此对外文案应写：

> DuoLA AgentCost：本地 BYOK 的 Agent Gateway，先把请求、费用、预算和可解释处理证据收在用户自己机器上。

不能写：

> 自动省 30%/50% Token、同等 Edgee、任意 Agent 都能自动压缩、进程出故障也不影响原 Agent。

## 五、发布门禁

### P0：未完成前不能公开宣传“产品已上线”

- 用真实 OpenAI API 完成一次 Responses 非流式和 SSE 流式会话（LinkAPI OpenAI-compatible 已完成同类验证）；
- 用真实 Anthropic API 完成一次 Messages 非流式和 SSE 流式会话；
- 用真实 Codex 和 Claude Code 各跑一次只读任务，记录退出恢复（LinkAPI + Codex 已完成一轮，Claude Code 仍需验证）；
- 至少用一个真实 Cursor BYOK 配置验证请求是否真的经过 Gateway；
- 进行一次原始路径 vs Gateway 路径的固定任务双跑，记录 Provider usage、任务结果、耗时和账单；
- 真实双跑前，不发布任何固定节省百分比。

### P1：要成为 Edgee/Portkey 的完整直接替代品前仍需补齐

- 更广的重复标题/分页/固定包装 trimming；当前已有长重复行和安全工具面筛选；
- 语义缓存（当前有意不做默认启用，避免相似问题误复用）；
- 输出简洁化（当前只提供显式 output token cap，不自动改写模型输出）；
- 原始 token baseline 与压缩后 token 的同任务对照；
- 规则失败、预算拒绝、循环暂停进入账本，而不只是返回 HTTP 429；（预算/循环拒绝已进入账本，规则失败明细仍需补齐）
- Fallback attempt chain、最终 Provider、失败原因可查询；（本轮已完成本地 API 和 Dashboard 链路）
- Gateway 崩溃后的守护/重启/恢复机制；（`launch` 托管模式已完成有限自动重启，Gateway 已支持 SIGINT/SIGTERM 优雅退出；操作系统级守护仍需按平台发布）
- Linux/macOS/Windows CI 和发布包；
- 远程 Gateway 认证、请求体/响应体/空闲超时、客户端取消、缓存总量和硬 USD 预算的真实负向验收；
- `model_map` 已实现显式映射；仍需补充跨真实 Provider 的兼容性验证。
- 云端多租户、团队密钥、SSO、Guardrails 和跨团队控制面；

## 最终判断

**代码不是坏的；当前本地闭环也不是假的。** 本轮发布前硬边界已落到实现和自动化门禁；“所有真实 Provider/Agent、所有平台都已通过”仍不能在没有第三方账号和目标机器的情况下宣称。

准确结论是：

> DuoLA AgentCost 已完成一个可运行的本地 Agent Gateway 产品级 release candidate，核心转发、确定性压缩、精确缓存、路由、Token/USD 预算、资源上限、取消/超时、限流、账本、Fallback 语义保护和托管恢复路径已验证；真实 Agent/Provider 的持续兼容矩阵、跨平台安装包、语义缓存、自动输出改写和团队控制面仍是与完整云端竞品的差距。
