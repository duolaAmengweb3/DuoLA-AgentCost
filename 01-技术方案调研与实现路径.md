# DuoLA AgentCost 技术方案调研与实现路径

> 研究日期：2026-08-13  
> 适用项目：`05-DuoLA-AgentCost`  
> 目标：确认 Edgee 类 Agent Gateway 是否能被我们真实实现，明确不同 Coding Agent 的接入边界、技术路线、外部成本和硬验收标准。  
> 结论性质：技术方案，不代表已经完成开发或已经达到 Edgee 的效果。

---

## 一、技术结论先行

我们可以做 Edgee 类产品，但不能把它理解成一个简单的 HTTP 转发代理。

真正可落地的产品必须由四部分组成：

```text
本地启动器 / 配置适配器
          ↓
本地 Agent Gateway
          ↓
协议解析与安全上下文处理
          ↓
用户自己的模型 Provider
```

其中：

- **本地启动器**负责让不同 Agent 使用 AgentCost；
- **协议适配器**负责识别 Anthropic Messages、OpenAI Responses、Chat Completions 等请求；
- **上下文处理器**负责安全地处理工具结果、工具定义、历史和重复内容；
- **账本与控制器**负责 Token、费用、预算、延迟、重试和失败回退；
- **模型调用**默认仍然走用户自己的 Key 或订阅，不由 DuoLA 承担模型费用。

最重要的实现决策是：

> **先做本地透明网关，先处理确定性、可恢复的工具结果；不要一开始做远程 AI 摘要、模型托管和自动模型路由。**

---

## 二、Edgee 的公开技术事实

Edgee 官方公开的 Agent Gateway 模式是：网关位于 Agent 和模型供应商之间，通过 CLI 启动已有 Agent，不要求修改项目代码。它把能力分成 Compress、Route、Observe 三个部分。[Edgee 官方 Agent Gateway](https://www.edgee.ai/)

Edgee 的公开压缩文档将压缩分成两层：

- 输入层：工具结果、工具定义、代码上下文和历史；
- 输出层：模型生成的冗余表达。

其中工具结果处理被描述为删除重复标题、分页标记、ANSI 控制字符和冗余包装，而不是重新总结业务内容。[Edgee Token Compression](https://www.edgee.ai/docs/features/token-compression)

Edgee 对 Codex 的公开配置显示，它通过 `model_provider`、自定义 `base_url` 和 `wire_api = "responses"` 接入 OpenAI Responses 协议。[Edgee Codex Compression](https://www.edgee.ai/docs/features/codex-compression)

Edgee 还公开了一个基准仓库，用来对比不同压缩策略下的 Agent 会话 Token 和费用。这个仓库说明：真实验证需要固定仓库、固定任务、隔离运行环境和会话级统计，不能只看一次请求的 Token 数。[Edgee Compression Lab](https://github.com/edgee-ai/compression-lab)

这些公开资料可以作为架构参考，但不是我们可以直接复制的代码，也不能把 Edgee 自己公布的节省比例当作我们产品的结果。

### Edgee 的开源边界

这点必须单独说清楚：**Edgee 不是整个项目开源，也不是把生产 Gateway 放在 GitHub 上让别人直接部署。**

Edgee 官方 `edgee-ai/edgee` 仓库是公开的 Rust CLI，仓库 README 明确写明：

- CLI 负责安装、登录、启动和配置 Claude Code、Codex、Cursor 等客户端；
- 生产 Gateway 的路由、压缩、计量、计费和观测不在该仓库；
- 该仓库本身不支持自托管生产 Gateway；
- 仓库使用 Apache-2.0 许可证。[Edgee CLI 仓库](https://github.com/edgee-ai/edgee)

Edgee 的公开 CLI 源码可以看到几种重要接入方式：

1. CLI Agent 通过环境变量或 Provider 配置指向 Gateway；
2. 桌面应用通过本地 relay 处理无法直接改 Provider URL 的场景；
3. Codex Desktop 通过启动前临时修改 `config.toml`，启动后恢复配置；
4. 不同 Agent 使用不同的启动器，而不是一个通用命令强行覆盖所有客户端。

这些实现细节非常值得我们参考，但不能把它们误解为 Edgee 的压缩引擎本身已经开源。

Edgee 还公开了 `compression-lab`，许可证为 Apache-2.0。这个仓库是基准测试工具，不是生产压缩 Gateway；它要求用 Edgee CLI 启动 Claude/Codex，再对比不同场景下的会话统计。[Edgee Compression Lab](https://github.com/edgee-ai/compression-lab)

Edgee 官方还明确说明，其 Gateway 的工具结果 trimming 最初基于 RTK，并将策略集成到自己的 Rust Gateway；因此我们应优先研究 RTK 这类开源本地工具输出压缩项目，再自行实现协议网关和产品层，而不是试图“复制 Edgee 的闭源生产服务”。[Edgee Why Edgee](https://www.edgee.ai/docs/introduction/why-edgee)

### 对 DuoLA 的直接启示

我们可以合法、合理地参考：

- CLI 启动器和 Agent 适配器的拆分方式；
- 配置快照、临时注入和启动后恢复；
- CLI Agent 与桌面 App 使用不同 Transport；
- 本地 relay 的 Fail Open 思路；
- benchmark 的固定任务、隔离运行和会话级账本；
- RTK 等 Apache-2.0 项目的可复用组件和思路，前提是保留许可证和修改记录。

我们不能直接复制：

- Edgee 的生产 Gateway；
- Edgee 的压缩分类器和内部策略；
- Edgee 的服务端路由、计费和观测实现；
- Edgee 的私有模型或训练数据；
- Edgee 的代码、品牌、接口文案和未公开实现。

因此，本项目不会按“照抄 Edgee 源码”推进，而是按以下路径实现：

```text
开源参考：RTK + Edgee CLI 的适配/启动思路
                 ↓
我们自己实现：DuoLA 本地 Gateway + 协议解析 + 账本 + 安全策略
                 ↓
我们自己验证：真实 Codex / Claude Code / Cursor 双跑
```

---

## 三、不同 Agent 的真实接入边界

### 1. Claude Code：适合 API Gateway 模式

Anthropic 官方提供了 LLM Gateway 配置文档，明确支持通过 `ANTHROPIC_BASE_URL` 将 Claude Code 接到统一的兼容端点，并用于认证、用量跟踪、预算和路由。[Anthropic LLM Gateway 配置](https://docs.anthropic.com/en/docs/claude-code/llm-gateway)

因此，Claude Code 的 API 模式可以使用：

```bash
export ANTHROPIC_BASE_URL=http://127.0.0.1:8787
export ANTHROPIC_API_KEY=用户自己的Key
claude
```

但要区分认证方式：Claude Code 可以使用 Anthropic Console、Claude Pro/Max 或企业云平台认证。不同认证路径不一定都允许我们以同样方式插入本地网关。官方文档显示 Pro/Max 与 Console API 是不同的认证选项，因此不能承诺所有订阅路径都能被透明代理。[Claude Code 安装与认证](https://docs.anthropic.com/en/docs/claude-code/getting-started)

**产品结论**：

- API Key / Anthropic-compatible endpoint：可优先支持；
- Bedrock / Vertex：通过对应协议适配，后置；
- Claude Pro/Max 原生订阅：必须单独实测，不承诺强行拦截。

### 2. Codex CLI：API Provider 模式可接，ChatGPT 订阅路径要单独处理

OpenAI Codex 的公开配置 Schema 包含：

- `openai_base_url`；
- `model_provider`；
- `model_providers.<name>.base_url`；
- `wire_api`；
- `model_providers` 自定义 Provider。

这说明 Codex 的 OpenAI API / OpenAI-compatible Provider 模式具备自定义网关入口。[Codex 配置 Schema](https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json)

Codex 同时存在 OpenAI API 和 ChatGPT 后端路径，公开 Schema 将 `openai_base_url` 与 `chatgpt_base_url` 分开定义。因此不能把“支持 OpenAI API Provider”误写成“支持所有 ChatGPT 订阅流量”。[Codex 配置源码](https://github.com/openai/codex/blob/main/codex-rs/config/src/config_toml.rs)

Codex 使用 Responses wire API 时，网关必须完整支持：

- 流式 Responses 事件；
- 工具调用和工具结果；
- reasoning 相关字段；
- response 续接和中断；
- usage 统计；
- 错误结构和重试语义。

**产品结论**：

- OpenAI API Key / compatible Provider：优先支持；
- Codex ChatGPT 订阅后端：不作为正式版本承诺；
- 不通过抓取或模拟 ChatGPT 私有接口实现接入。

### 3. Cursor：MCP 可控，内置模型流量不能假设全部可拦截

Cursor 官方文档显示：

- 用户可以配置 OpenAI、Anthropic、Google、Azure、Bedrock 等自有 API Key；
- 自定义 API Key 只适用于标准聊天模型；
- Tab Completion 等专用模型仍使用 Cursor 内置模型；
- Cursor 支持本地和远程 MCP，通过 `mcp.json`、stdio、SSE 和 Streamable HTTP 接入。[Cursor API Keys](https://docs.cursor.com/settings/api-keys)、[Cursor MCP](https://docs.cursor.com/context/model-context-protocol)

因此 Cursor 必须分成两条路线：

1. **用户自带 API Key 的标准模型路径**：可以通过自定义 Provider 或本地网关验证；
2. **Cursor 内置模型和专用功能**：不能假设我们可以拦截，除非 Cursor 官方提供明确的自定义网关入口。

Cursor 的 MCP 工具可以通过本地网关或 Tool Surface 适配器控制，但这不是同一件事，不能把 MCP 代理冒充成完整模型网关。

**产品结论**：Cursor 是“部分支持”，必须在 UI 和官网明确说明支持边界。

### 4. OpenCode：适合 OpenAI-compatible Gateway

Edgee 官方已经给出 OpenCode 通过 OpenAI-compatible `baseURL` 接入的配置示例。[Edgee OpenCode 集成](https://www.edgee.ai/docs/integrations/opencode)

OpenCode 适合作为协议兼容测试客户端，因为接入方式清晰、不会强绑定某个订阅系统。

---

## 四、为什么必须采用“网关 + 启动器”而不是单一代理

不同 Agent 的差异不只是 URL：

| 差异 | 影响 |
|---|---|
| Anthropic Messages 与 OpenAI Responses 格式不同 | 请求和流式事件不能直接透传后统一处理 |
| API Key 与订阅 OAuth 不同 | 有些用户路径能代理，有些不能代理 |
| Cursor 内置模型不开放相同接口 | 不能承诺全量拦截 |
| MCP 工具由客户端管理 | 工具 Schema 不一定全部经过同一个模型请求入口 |
| Agent 自动更新 | 请求字段、错误码和认证流程可能变化 |
| 不同客户端对流式中断和重试处理不同 | 网关错误可能放大为重复调用或会话损坏 |

所以产品需要：

```text
AgentCost CLI
  ├── codex adapter
  ├── claude adapter
  ├── cursor adapter
  ├── opencode adapter
  └── generic OpenAI-compatible adapter

Local Gateway
  ├── Anthropic Messages parser
  ├── OpenAI Responses parser
  ├── Chat Completions parser
  ├── MCP metadata observer
  └── fail-open transport layer
```

---

## 五、推荐的系统架构

### 1. 本地进程组成

建议使用 Go 实现本地网关和 CLI：

- 单一二进制，便于 macOS、Linux、Windows 分发；
- HTTP、SSE、WebSocket 和文件系统支持成熟；
- 常驻内存可控；
- 不要求用户安装 Node、Python 或 Rust 运行环境；
- 更适合做本地守护进程和系统托盘服务。

Edgee 官方选择 Rust 网关，是低延迟和跨平台实现的可行证据。我们的正式版本直接采用 Rust 单二进制，避免在生产发布后再承担运行时迁移成本。

### 2. 进程结构

```text
duola-agentcost
  ├── CLI
  │   ├── install
  │   ├── launch codex
  │   ├── launch claude
  │   ├── launch cursor
  │   ├── status
  │   ├── bypass
  │   └── diagnose
  │
  ├── Local Gateway
  │   ├── HTTP server
  │   ├── SSE passthrough
  │   ├── WebSocket passthrough (if required)
  │   ├── provider adapters
  │   ├── auth/header pass-through
  │   └── request correlation
  │
  ├── Policy Engine
  │   ├── project policy
  │   ├── safe transformations
  │   ├── budget policy
  │   ├── retry loop policy
  │   └── fail-open policy
  │
  ├── Context Processor
  │   ├── tool result trimming
  │   ├── repeated framing removal
  │   ├── ANSI / pagination cleanup
  │   ├── structured output handling
  │   ├── tool surface observer
  │   └── optional local classifier
  │
  ├── Ledger
  │   ├── request metadata
  │   ├── token estimates
  │   ├── provider usage
  │   ├── cost calculation
  │   ├── transformation receipts
  │   └── baseline comparison
  │
  └── Local Dashboard
      ├── localhost web UI
      ├── current session
      ├── project cost
      ├── saved tokens
      ├── policy controls
      └── bypass / recovery
```

### 3. 本地数据存储

建议使用 SQLite：

```text
projects
sessions
requests
request_usage
transformations
budget_events
provider_profiles
benchmark_runs
```

默认只保存：

- 会话和请求 ID；
- 客户端、模型和 Provider；
- Token、费用、耗时、状态码；
- 使用了哪些规则；
- 原文哈希和内容位置；
- 失败和回退原因。

默认不保存完整 Prompt、代码、日志和 Completion。用户明确开启本地调试模式后，才可在用户设备保存原文快照。

---

## 六、请求处理管线

### 1. 接收与识别

网关首先识别：

- 请求路径；
- HTTP 方法；
- Provider；
- wire protocol；
- 模型；
- 是否流式；
- 是否包含工具定义；
- 是否包含工具结果；
- 是否包含 Responses item。

未知字段必须原样保留，不能因为解析器不认识就删除。

### 2. 内容分层

把请求拆成：

```text
system / developer instructions
user messages
assistant messages
tool definitions
tool calls
tool results
reasoning / hidden metadata
attachments / images / files
```

正式版本只处理能明确识别的 `tool_result`、Shell 输出、搜索输出和重复包装。用户指令、系统指令、代码补丁和结构化工具参数默认不改。

### 3. 确定性安全处理

允许处理：

- 去除 ANSI 控制字符；
- 去除重复标题和分页标记；
- 去除固定工具包装层；
- 保留命令输出的错误、退出码和关键路径；
- 保留代码行号和文件名；
- 对重复的表头和固定元数据去重；
- 为大型结果建立本地引用，但必须保留恢复路径。

正式版本禁止：

- 直接让远程 LLM 总结每个工具结果；
- 随意截断 JSON；
- 改写用户输入；
- 改写系统提示词；
- 删除看似无关但无法证明无关的代码；
- 自动压缩 reasoning 内容；
- 在未验证的情况下删除工具定义。

### 4. 工具 Surface 处理

工具按需加载是 Edgee 的 alpha 能力，官方方案使用小型分类器根据任务和工具描述打分，再筛掉无关工具。[Edgee Token Compression](https://www.edgee.ai/docs/features/token-compression)

我们应该分两步实现：

1. 先做观察模式：记录每个任务实际调用的工具，计算理论可减少的工具 Schema；
2. 再做显式开启的策略模式：只对用户配置的工具组、固定名称和高置信度规则做延迟加载；
3. 最后才考虑本地小模型分类器；
4. 任何低置信度结果都保留工具，不强行筛掉。

这样可以避免一开始因工具筛错导致 Agent 不知道自己有能力完成任务。

### 5. 输出处理

输出压缩不是正式版本核心，因为它涉及模型行为改变。Edgee 文档也将输出简洁模式设为可选能力，并指出 Coding Agent 的主要 Token 体量在输入侧。[Edgee Claude Code Compression](https://www.edgee.ai/docs/features/claude-compression)

正式版本只记录输出长度和成本，不修改模型输出。后续如果加入 output brevity，必须：

- 默认关闭；
- 只处理固定冗余前缀和重复说明；
- 不处理代码、命令、JSON、补丁和错误信息；
- 支持对照测试。

---

## 七、流式、重试和错误处理

### 1. 流式响应

网关必须支持：

- HTTP chunked response；
- Server-Sent Events；
- OpenAI Responses 事件流；
- Anthropic event stream；
- 客户端主动取消；
- 上游半途断开；
- usage 在结尾事件中返回；
- 连接关闭后的状态落账。

原则是：

> 网关只处理发送给模型前的输入上下文；除非明确开启 output 策略，否则不缓冲和改写输出流。

### 2. 重试

不能把所有错误都重试。必须按请求类型区分：

- 连接建立失败：有限次重试；
- 429：遵守 Retry-After 和 Provider 限额；
- 5xx：有限次、带抖动重试；
- 流式响应已经产生内容后断开：不得盲目重放；
- 用户取消：不得重试；
- 参数错误：不得重试；
- 认证失败：不得重试。

每次重试必须单独记账，不能把失败请求隐藏在一个“成功”里。

### 3. Fail Open

当发生以下情况时，默认绕过优化层，将原始请求直接转发到用户 Provider：

- 解析器不认识新字段；
- 压缩超时；
- 本地数据库不可用；
- 策略校验失败；
- 上下文恢复映射缺失；
- 网关自身异常。

只有用户主动开启“严格预算阻断”时，预算超限才允许阻塞请求。

---

## 八、Token 与费用统计

### 1. 统计分层

每次请求记录四类数值：

```text
estimated_input_tokens
sent_input_tokens
provider_reported_input_tokens
provider_reported_output_tokens
```

其中：

- 本地预估值用于决定是否处理；
- 实际发送值用于比较压缩比例；
- Provider 返回值作为账单主要依据；
- 如果 Provider 不返回完整 usage，必须标记为估算，不得伪装成精确账单。

### 2. 费用计算

费用公式至少区分：

```text
input_cost
output_cost
cached_input_cost
retry_cost
compression_cost
total_provider_cost
```

不同 Provider、不同模型和不同缓存政策必须有版本化价格表。价格表变化后，历史账单不能被静默重算。

### 3. 省钱的真实定义

```text
实际节省金额
= 基线任务 Provider 成本
 - AgentCost 任务 Provider 成本
 - AgentCost 自身产生的额外模型成本
```

如果使用远程分类器或摘要模型，必须把它产生的费用计入 AgentCost 成本，不能把这部分隐藏。

---

## 九、CLI 与接入方式

### 1. 推荐命令

```bash
duola-agentcost install
duola-agentcost doctor
duola-agentcost launch codex
duola-agentcost launch claude
duola-agentcost launch cursor
duola-agentcost status
duola-agentcost bypass
duola-agentcost uninstall
```

### 2. 启动器做什么

启动器不应该复制或接管用户的 Key，而是：

- 启动本地网关；
- 注入临时 Provider URL；
- 继承用户原来的环境变量；
- 保存原始配置快照；
- 退出后恢复原设置；
- 记录客户端和版本；
- 发现冲突时先停止，不覆盖用户配置。

### 3. Codex 接入示例

Codex 的配置支持自定义 Provider，因此可以生成一个本地 Provider：

```toml
model_provider = "duola-agentcost"

[model_providers.duola-agentcost]
name = "DuoLA AgentCost"
base_url = "http://127.0.0.1:8787/v1"
wire_api = "responses"
env_key = "OPENAI_API_KEY"
```

实际字段必须跟随当时 Codex 的配置 Schema 进行版本测试，不能只按文档示例写死。

### 4. Claude Code 接入示例

API 模式可使用：

```bash
ANTHROPIC_BASE_URL=http://127.0.0.1:8787 \\
ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" \\
claude
```

如果用户使用的是 Pro/Max 原生订阅，必须先通过真实版本验证认证链路是否能被本地网关承接。

### 5. Cursor 接入示例

Cursor 的 API Key 路径支持部分自定义 Provider；Cursor 的 MCP 则可通过 `.cursor/mcp.json` 配置本地 stdio 或 HTTP 服务。[Cursor MCP](https://docs.cursor.com/context/model-context-protocol)

不能在产品文案中写“完全支持 Cursor”，应该明确：

- 自带 API Key 的标准模型：验证后支持；
- Cursor 内置模型和专用功能：不承诺可拦截；
- Cursor MCP：可通过 AgentCost 的本地 MCP 网关治理。

---

## 十、隐私和 Key 设计

### 默认数据边界

```text
用户设备内：
  Prompt / Completion / 代码 / 日志 / Key / 原文索引

DuoLA 云端默认只接收：
  版本检查 / 许可证状态 / 匿名化统计（可选）
```

### Key 规则

- 不将用户 Provider Key 写入 DuoLA 数据库；
- 不通过云端控制面转发用户请求；
- 本地 Key 优先从环境变量、系统密钥链或原有 Agent 配置读取；
- 日志中禁止出现 Authorization、Cookie、API Key；
- `doctor` 命令必须能扫描并报告潜在 Key 泄露；
- 诊断包默认只包含元数据，不包含 Prompt 和代码。

### 调试模式

只有用户明确开启本地调试模式，才允许在设备上保存原始请求。调试文件：

- 必须有明确过期时间；
- 必须提供删除命令；
- 必须标记为敏感数据；
- 不自动同步到云端。

---

## 十一、外部硬成本评估

### 本地优先模式

DuoLA 不承担用户每次模型推理费用，运行时没有按请求发生的模型成本。主要外部支出是：

- 官网和文档托管；
- CLI 二进制发布和签名；
- 版本更新服务；
- 可选的错误监控；
- 可选账号、授权和订阅系统。

这类成本与用户使用次数不直接线性增长，适合低成本启动。

### 云端代理模式

如果将 Prompt 和 Completion 经过 DuoLA 云端，则会增加：

- 网关计算；
- 带宽；
- 日志和指标存储；
- 高可用和故障转移；
- 数据保护和合规责任。

### 模型托管或远程压缩模式

如果用远程模型来做工具分类或摘要，还要增加：

- 压缩模型推理费用；
- 额外延迟；
- 额外数据传输；
- 压缩本身导致的 Token 成本；
- 结果漂移和质量验证成本。

**成本结论**：

> 先做本地网关和确定性处理，外部硬成本可控；一旦做云端模型代理或模型 fallback，成本和责任会显著上升。

---

## 十二、真实测试方法

### 1. 基准任务集

建立固定但不公开全部答案的本地任务集：

- 小型单文件修改；
- 跨文件功能；
- 测试失败排查；
- 大日志定位；
- 大仓库搜索；
- 数据库迁移；
- 多工具/MCP 任务；
- 反复报错任务；
- 长时间多轮任务。

### 2. 双跑结构

```text
同一 Agent 版本
同一模型
同一仓库快照
同一用户目标

Run A：直连 Provider
Run B：经过 DuoLA AgentCost
```

每次双跑都保存：

- Git diff；
- 测试结果；
- 最终任务状态；
- 工具调用序列；
- 请求和响应数量；
- Provider usage；
- 费用；
- 延迟；
- 重试和回退；
- AgentCost 处理记录。

### 3. 测试分层

#### 协议测试

- Anthropic Messages；
- OpenAI Responses；
- Chat Completions；
- SSE 事件；
- 中断和取消；
- usage 结尾事件；
- 错误透传。

#### 变换测试

- ANSI；
- 重复头；
- 分页；
- 日志；
- JSON；
- 代码；
- 搜索结果；
- 数据库结果；
- 工具 Schema。

#### 故障测试

- 上游 429；
- 上游 5xx；
- 网络断开；
- 本地进程崩溃；
- SQLite 锁；
- 超时；
- Provider Key 错误；
- 压缩处理超时。

#### 真实任务测试

- 任务是否完成；
- 测试是否通过；
- 代码行为是否变化；
- 实际费用是否下降；
- Agent 是否出现新增循环；
- 用户是否仍然可以绕过。

---

## 十三、硬验收门槛

### 接入门槛

- 至少一个 Anthropic API 模式和一个 OpenAI Responses 模式跑通；
- Codex 和 Claude Code 至少各完成一套真实端到端测试；
- Cursor 必须明确支持边界，不得用模拟结果宣称全支持；
- 不改用户项目代码即可安装、启用和关闭；
- 启动器不会破坏用户原配置。

### 正确性门槛

- 未识别字段 100% 原样保留；
- 工具参数不得被静默修改；
- 代码、JSON、补丁和错误堆栈默认不做有损摘要；
- 任何变换都有可追溯记录；
- 任意变换都能关闭或绕过；
- 失败开放路径可重复验证。

### 成本门槛

- Provider usage 优先于本地估算；
- Provider 不提供精确 usage 时明确标注估算；
- 压缩模型成本必须计入；
- Prompt Cache 单独统计；
- 重试成本不能隐藏；
- 历史账单不因价格表更新而静默改变。

### 性能门槛

- 本地网关自身 P50 额外延迟可测；
- SSE 不因网关缓冲造成明显体验下降；
- 常规任务不产生明显 CPU 和内存异常；
- 网关关闭后 Agent 仍可直接运行；
- 大输出处理不会让 Agent 长时间卡住。

### 价值门槛

只有在真实双跑中同时满足以下条件，才能宣称产品有价值：

- 实际 Provider 费用有稳定下降；
- 上下文过长和无效重复调用减少；
- 任务完成率和测试结果没有可重复下降；
- 用户能解释节省来自哪里；
- 用户愿意持续开启，而不是只测试一次。

---

## 十四、实现顺序

### 第一步：协议和客户端事实确认

- 固定 Codex、Claude Code、Cursor、OpenCode 版本；
- 验证 API Provider 入口；
- 验证流式和工具调用；
- 验证用户配置能否安全恢复；
- 记录不能接入的订阅路径。

### 第二步：本地网关和启动器

- Go 单二进制；
- 本地端口和健康检查；
- 启动器和配置快照；
- 代理关闭和 bypass；
- 基础请求转发；
- 无原文日志。

### 第三步：账本和观察模式

- request/session/project 关联；
- Provider usage 采集；
- 费用计算；
- 工具结果长度和重复度统计；
- 只观察、不变换的基线报告。

### 第四步：确定性工具结果处理

- ANSI 和固定包装清理；
- 重复头和分页清理；
- 大日志的安全保留规则；
- 原文哈希和恢复映射；
- 规则逐项开关；
- 双跑对照。

### 第五步：预算和 fail-open

- 单任务和项目预算；
- 重复错误检测；
- 有限重试；
- 超限暂停；
- 本地进程异常绕过；
- 用户可见原因。

### 第六步：工具 Surface 观察和可选策略

- 先统计实际工具使用；
- 再做固定工具组和高置信度规则；
- 最后评估本地小模型分类；
- 不把分类器当产品成立前提。

### 第七步：本地 Dashboard 和真实用户验证

- 费用和 Token 对比；
- 任务报告；
- 变换和恢复记录；
- 预算和策略控制；
- 真实用户双跑。

---

## 十五、当前不能承诺的事情

在真实技术验证前，不能对外承诺：

- 支持全部 Coding Agent；
- 支持 Claude Pro/Max、ChatGPT Plus/Pro 等所有订阅路径；
- 固定节省 30%、50% 或更多；
- 压缩后绝对不影响任何任务；
- Cursor 内置模型请求一定可拦截；
- 能自动 fallback 到其他模型；
- 云端零数据风险。

可以承诺的只有设计目标：

> 本地优先、BYOK、可绕过、可恢复、真实记账、先保证正确，再优化成本。

---

## 十六、技术方案最终判断

### 能不能做

能。公开证据已经证明 Agent Gateway 的接入路径和上下文处理路径存在：

- Anthropic 官方支持 Claude Code 接入 LLM Gateway；
- Codex 公开配置支持自定义 OpenAI Provider 和 Responses wire；
- Cursor 支持自有 API Key 和本地/远程 MCP；
- Edgee 已经公开了透明 CLI、协议适配和工具结果压缩的产品形态。

### 应该怎么做

```text
第一核心：本地透明网关
第二核心：协议适配和真实账本
第三核心：确定性、可恢复的工具结果处理
第四核心：预算、循环识别和 fail-open
第五核心：观察后再做工具 Surface Reduction
```

### 不应该怎么做

- 不先做云端模型代理；
- 不先做模型 fallback；
- 不先用远程大模型总结用户上下文；
- 不先承诺全部 Cursor、Codex、Claude 订阅支持；
- 不用压缩比例代替任务正确率；
- 不把本地产品做成必须登录和持续联网才能使用的服务。

## 参考资料

- [Edgee Agent Gateway](https://www.edgee.ai/)
- [Edgee Token Compression](https://www.edgee.ai/docs/features/token-compression)
- [Edgee Codex Compression](https://www.edgee.ai/docs/features/codex-compression)
- [Edgee Claude Code Compression](https://www.edgee.ai/docs/features/claude-compression)
- [Edgee Compression Lab](https://github.com/edgee-ai/compression-lab)
- [Anthropic LLM Gateway Configuration](https://docs.anthropic.com/en/docs/claude-code/llm-gateway)
- [Anthropic Claude Code Authentication](https://docs.anthropic.com/en/docs/claude-code/getting-started)
- [OpenAI Codex Config Schema](https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json)
- [OpenAI Codex Config Source](https://github.com/openai/codex/blob/main/codex-rs/config/src/config_toml.rs)
- [Cursor API Keys](https://docs.cursor.com/settings/api-keys)
- [Cursor MCP](https://docs.cursor.com/context/model-context-protocol)
- [Edgee OpenCode Integration](https://www.edgee.ai/docs/integrations/opencode)
